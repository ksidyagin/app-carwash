export interface NewsModelServer {
    id: number;
    pic: string;
    title: string;
    description: string;
}
export interface SalesModelServer {
    id: number;
    pic: string;
    title: string;
    description: string;
}